@Library('todo') _

todo (
        COMPONENT             : 'todo',
        PROJECT_NAME          : "TODO",
        SLAVE_LABEL           : "master",
        SKIP_NEXUS_UPLOAD     : false,
        APP_TYPE              : "NODEJS"
)
